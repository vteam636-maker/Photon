# JARVIS execution manifest — this repo runs with a single command: sh run.sh
name: Photon
runtime:
  image: python:3.12-slim
  command: ["sh", "run.sh"]
  network: true
resources:
  memory_mb: 384
  cpu: 1.0
